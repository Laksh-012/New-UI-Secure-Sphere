<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Test.Pages.SettingsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Test.Services"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <ScrollViewer>
        <StackPanel Padding="24" Spacing="24">

            <!-- HEADER -->
            <TextBlock
                Text="Settings"
                FontSize="26"
                FontWeight="SemiBold"
                Foreground="White"/>

            <!-- GENERAL SETTINGS -->
            <Border Background="#1E1E1E"
                    CornerRadius="12"
                    Padding="16">
                <StackPanel Spacing="12">

                    <TextBlock
                        Text="General"
                        FontSize="18"
                        FontWeight="SemiBold"
                        Foreground="White"/>

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <TextBlock
                            Text="Enable automatic system scan"
                            Foreground="#AAAAAA"/>

                        <ToggleSwitch
                            Grid.Column="1"
                            />
                    </Grid>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <TextBlock Text="Dark Mode" 
                                   Foreground="#AAAAAA"/>
                        <ToggleSwitch Grid.Column="1"
                                      Header="Dark Mode"
                                      />

                    </Grid>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <TextBlock
                            Text="Start protection at system startup"
                            Foreground="#AAAAAA"/>

                        <ToggleSwitch
                            Grid.Column="1"
                            />
                    </Grid>

                </StackPanel>
            </Border>

            <!-- NOTIFICATIONS -->
            <Border Background="#1E1E1E"
                    CornerRadius="12"
                    Padding="16">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock
                        Text="Enable notifications"
                        Foreground="#AAAAAA"/>

                    <ToggleSwitch
                        Grid.Column="1"
                        />
                </Grid>
            </Border>

            <!-- ACCOUNT -->
            <Border Background="#1E1E1E"
                    CornerRadius="12"
                    Padding="16">
                <StackPanel Spacing="8">

                    <TextBlock
                        Text="Account"
                        FontSize="18"
                        FontWeight="SemiBold"
                        Foreground="White"/>

                    <TextBlock
                        Text="Signed in as"
                        Foreground="#AAAAAA"/>

                    <TextBlock
                        Text="Laksh5665@outlook.com"
                        Foreground="White"
                        FontWeight="SemiBold"/>

                </StackPanel>
            </Border>

        </StackPanel>
    </ScrollViewer>
</Page>